{% extends 'basic.twig' %}
{% block title %}Administration - ajouter un serveur | {{ parent() }}{% endblock %}
{% block body %}
    <div class="col-md-12">
        <form method="POST" action="/admin/addserver">
            <div class="form-group">
                <label for="name">Nom</label>
                <input class="form-control" id="name" name="name" maxlength="255"/>
            </div>
            <div class="form-group">
                <label for="url">Url</label>
                <input class="form-control" id="url" name="url" maxlength="255"/>
            </div>
            <div class="form-group">
                <label for="name">Cron</label>
                <input class="form-control" id="cron" name="cron" maxlength="30"/>
            </div>
            <div class="form-check">
                <input class="form-check-input" type="checkbox" value="true" id="disabled" name="disabled">
                <label class="form-check-label" for="disabled">
                    Désactiver
                </label>
            </div>
            <button class="btn btn-primary" type="submit">Envoyer !</button>
        </form>
    </div>
{% endblock %}
